function int plus(int a, int b) {
    if (a + b == 3) {
        return 2;
    } else {
        int x;
        x = a - b;
        return a + b + x % 5;
    }
    if ((a + b < a || a > 233) && !(b != a)) {
        return plus(b, b - a);
    }
}
struct Data {
    int x;
};
struct Car {
    int height;
    float *weight;
    struct Data data[12][13];
};
struct Node {
    int from, to;
    float weight;
    struct Car car;
};
int ga, gb;
function int main()
{
    int x, *y, i;
    while (i <= 10 && x > 3) {
        i = i + 1;
        int z;
    }
    int tmp;
    tmp = (2 + i) * 2 + x / 3;
    int arr[10][20][30];
    arr[4][5][6] = arr[6][5][4];
    struct Car car;
    arr[9][8][7] = car.data[1][2].x;
    struct Node node;
    int res;
    res = plus(1, arr[0][0][0]);
    node.from = 2;
}